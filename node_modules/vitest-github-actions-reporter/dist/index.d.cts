import { Reporter, Vitest, File } from 'vitest';

type GitHubActionsReporterOptions = {
    /**
     * @default true
     */
    trimRepositoryPrefix?: boolean;
    /**
     * @default false
     */
    hideStackTrace?: boolean;
};
declare class GitHubActionsReporter implements Reporter {
    ctx: Vitest;
    options: Required<GitHubActionsReporterOptions>;
    constructor({ trimRepositoryPrefix, hideStackTrace }?: GitHubActionsReporterOptions);
    onInit(ctx: Vitest): void;
    onFinished(files?: File[]): Promise<void>;
    private reportFiles;
    private reportTasks;
    private reportTaskErrors;
    private reportTaskError;
    private parseStacktrace;
    private createMessage;
    private getPositionFromError;
    private getErrorTitle;
}

export { type GitHubActionsReporterOptions, GitHubActionsReporter as default };
